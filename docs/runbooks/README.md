# Runbooks

Short, concrete, executable walkthroughs — a reader should be able to follow one and actually perform
the task, referencing real class/story names rather than speaking abstractly.

Populated incrementally:

- `local-dev-setup.md` — STORY-008 (`devops-observability-engineer`): `docker compose up` → migrate →
  run.
- Rotating the active LLM provider, adding a new file loader, adding a new payment provider —
  STORY-066 (`docs-writer`).

See [.claude/rules/documentation-standards.md](../../.claude/rules/documentation-standards.md).
